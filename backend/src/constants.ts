export const reqString = {
  type: String,
  required: true,
};

export const reqNumber = {
  type: Number,
  required: true,
};

export const defaultNumber = {
  type: Number,
  required: true,
  default: 0,
};
